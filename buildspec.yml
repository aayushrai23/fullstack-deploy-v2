version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - cd frontend
      - npm install --legacy-peer-deps
  build:
    commands:
      - echo "Building app..."
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: frontend/build
